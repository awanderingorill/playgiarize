<div class="col-md-12">
<% if @pieces.any? %>
  <h2>All pieces:</h2>
  <ul>
    <% @pieces.each do |piece| %>
    <li><%= link_to("#{piece.title}", "/pieces/#{piece.id}") %></li>
    <% end %>
  </ul>
<% else %>
  No pieces here.
<% end %>
</div>